# SPEC — Sparker 플러그인 + 훅

> DSOT 하위 문서. 상위 문서 `CONSTITUTION.md`를 먼저 읽을 것.
> 각 항목은 ①목적 ②범위 ③명시적 비포함 ④수용 기준 ⑤검증 시나리오를 갖춘다.

**프로젝트 v1 완료 정의**: 23인이 설치 가능한 플러그인이 배포돼 있고, 훅이 보낸 사용량이 리더보드에 표시된다.
**기한**: 2026-10-07
**타깃**: S0

## S0. 플러그인 + 훅 v1
- 목적: 참가자가 설치하면 사용량이 자동으로 리더보드에 반영된다.
- 범위: 플러그인 본체, 사용량 훅, 설치 스크립트.
- 명시적 비포함: 참가자 코드·프롬프트 내용 수집. 집계값만 전송한다.
- 수용 기준: 깨끗한 환경에서 설치 후 사용량이 리더보드에 나타난다.
- 검증 시나리오: 별도 계정·환경에서 설치 → 작업 1회 → 리더보드 반영까지 실제로 왕복한다.

## S1. 설치 안내 탭
- 목적: 참가자가 막히지 않고 설치를 끝낸다.
- 범위: @xavier 2026-09-02 "플러그인을 설치하는 과정을 좀 정리해가지고 그 대시보드 페이지에 따로 탭 하나 만들어 주려고."
- 명시적 비포함: 사내 IT 정책 변경 요청. 그것은 메디트 발신 게이트다.
- 수용 기준: 안내만 보고 설치를 끝낸 사람이 나온다.
- 검증 시나리오: 설치 경험이 없는 1인이 문서만으로 완료한다.

## S2. 교육 지원 센서 (DEL-400 / DEL-472)
- 참가자 앱 사용량 연결의 개인 토큰을 재사용하고 명시적 `--consent`와 주차를 선택한다. 운영자 키는 배포하지 않는다.
- OTel 기본: `node sensor.mjs run`으로 Claude를 시작하면 프로세스 한정 로컬 수신기가 tool_result를 허용 필드로 정규화한다. 원문 로깅 옵션을 모두 끄며 외부 OTel destination을 전달하지 않는다. 회사 관리 정책이 이를 막으면 연결 미확인으로 남긴다.
- Hook fallback: `install --consent w1 --hook-fallback`으로 본인 훅 두 개만 추가한다. 기존 설정과 다른 훅을 유지한다. transcript 파일은 열지 않는다.
- 재전송은 정규화 사건만 500건·24시간까지 개인 파일(0600)에 대기한다. 서버는 승인 정책 만료와 사건 시점 7일 중 빠른 시점까지 100,000건 한도로 저장한다. 정책 승인이 없으면 수집을 시작하지 않는다.
- 검증: 합성 설치 환경에서 OTel launcher → 개인 인증 HTTP API → 운영자 export, 동의 누락·개인 식별 위조·원문 필드·중복 충돌·계정/토큰 만료·정책 만료. 실제 Claude 인증 세션 증거와 합성 테스트를 구분한다.
