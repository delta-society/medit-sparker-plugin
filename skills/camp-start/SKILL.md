---
name: camp-start
description: 기존 캠프 시작 명령입니다. start와 같은 준비·기록 확인·실습 흐름을 제공합니다.
argument-hint: "1 class | 1 homework"
---

Compatibility entry for `/sparker-camp:start`. Read the sibling `../start/SKILL.md` relative to this skill's installed directory, then follow its full flow with the original `$ARGUMENTS` and `${CLAUDE_SESSION_ID}`. Do not ask the participant to run another command solely because they used this alias. Resolve bundled helpers relative to this plugin's root, not the current project. Preserve explicit collection consent, safe terminal-only account connection, session-scoped receipt status, and the Discovery handoff described there.
